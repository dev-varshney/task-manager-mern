#Tast Manager MERN by Dev Varshney
MERN Task Manager - Dev Varshney
Run `npm install` in both frontend and backend folder to install all the dependencies
Run `npm start` in frontend
Run `npm run dev` in backend
